{/* eslint-disable prefer-arrow/prefer-arrow-functions, import/no-default-export */}
import '../styles.css';

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
